services:
  configserver:
    image: "eazybytes/configserver/s6"
    container_name: "configserver-microservice"
    ports:
      - "8071:8071"
    deploy:
      resources:
        limits:
          memory: 700m
    networks: # Must provided the same network name to the services communicate each others
      - eazybank
  accounts:
    image: "eazybytes/accounts/s6"
    container_name: "accounts-microservice"
    ports:
      - "8080:8080"
    deploy:
      resources:
        limits:
          memory: 700m
    networks:
      - eazybank
    environment:
      SPRING_CONFIG_IMPORT: "configserver:http://configserver:8071/"
      SPRING_PROFILES_ACTIVE: "default"
      SPRING_APPLICATION_NAME: "accounts"
  loans:
    image: "eazybytes/loans/s6"
    container_name: "loans-microservice"
    ports:
      - "8080:8080"
    deploy:
      resources:
        limits:
          memory: 700m
    networks: # Must provided the same network name to the services communicate each others
      - eazybank
    environment:
      SPRING_CONFIG_IMPORT: "configserver:http://configserver:8071/"
      SPRING_PROFILES_ACTIVE: "default"
      SPRING_APPLICATION_NAME: "loans"
  cards:
    image: "eazybytes/cards/s6"
    container_name: "cards-microservice"
    ports:
      - "8080:8080"
    deploy:
      resources:
        limits:
          memory: 700m
    networks:
      - eazybank
    environment:
      SPRING_CONFIG_IMPORT: "configserver:http://configserver:8071/"
      SPRING_PROFILES_ACTIVE: "default"
      SPRING_APPLICATION_NAME: "cards"
networks:
  eazybank:
    driver: "bridge"